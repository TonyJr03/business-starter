export interface ModulesConfig {
  catalog: boolean;
  promotions: boolean;
  cart: boolean;
  whatsappOrdering: boolean;
  testimonials: boolean;
  faq: boolean;
  gallery: boolean;
  blog: boolean;
}
