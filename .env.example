# ── Supabase ────────────────────────────────────────────────────────────────
# Copia este archivo como .env.local y rellena los valores.
# Déjalos vacíos para operar en modo local-first (datos mock).
# En producción (Vercel) configúralos en el panel de variables de entorno.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=